---
import Layout from "./Layout.astro";
import { companyInfo } from "../config";
import { getFullPath } from "../utils/url";

interface Props {
  title: string;
  icon: string;
  description?: string;
  heroBackground?: string;
  image?: string;
}

const { title, icon, description = "", heroBackground = "", image = heroBackground } = Astro.props;
---

<Layout title={title} description={description} image={image}>
  <!-- Breadcrumb -->
  <nav class="bg-gray-50 border-b border-gray-200">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <ol class="flex items-center space-x-2 text-sm">
        <li>
          <a href={getFullPath("/")} class="text-gray-500 hover:text-gray-700">Trang chủ</a>
        </li>
        <li>
          <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"
            ></path>
          </svg>
        </li>
        <li>
          <a href={getFullPath("/dich-vu")} class="text-gray-500 hover:text-gray-700">Dịch vụ</a>
        </li>
        <li>
          <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"
            ></path>
          </svg>
        </li>
        <li class="text-gray-900 font-medium">{title}</li>
      </ol>
    </div>
  </nav>

  <!-- Hero Section -->
  <section
    class="relative py-12 md:py-20 bg-cover bg-top bg-no-repeat"
    style={heroBackground
      ? `background-image: url('${heroBackground.startsWith("/") ? getFullPath(heroBackground) : heroBackground}')`
      : ""}
  >
    <!-- Dark Overlay -->
    <div class="absolute inset-0 bg-gradient-to-r from-gray-800/85 via-gray-700/80 to-gray-900/85">
    </div>

    <div class="relative z-10 container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto">
        <div
          class="flex flex-col md:flex-row items-center md:items-start gap-6 text-center md:text-left"
        >
          <div
            class="flex-shrink-0 w-20 h-20 bg-white/30 backdrop-blur-md rounded-lg flex items-center justify-center border-2 border-white/40 shadow-xl"
          >
            <svg
              class="w-10 h-10 text-white drop-shadow-lg"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d={icon}></path>
            </svg>
          </div>
          <div class="flex-1">
            <h1 class="text-4xl sm:text-5xl font-bold mb-4 text-white drop-shadow-lg">{title}</h1>
            {description && <p class="text-xl text-blue-100 drop-shadow-md">{description}</p>}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Page Content -->
  <slot />

  <!-- CTA Section -->
  <section class="bg-gray-800 text-white py-12 md:py-16">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl font-bold mb-4">Bạn cần tư vấn thêm?</h2>
        <p class="text-xl text-blue-100 mb-8">
          Liên hệ ngay với chúng tôi để được tư vấn miễn phí và báo giá chi tiết
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href={getFullPath("/lien-he")}
            class="bg-white text-blue-700 px-8 py-3 rounded-lg font-semibold hover:bg-blue-50 transition-colors duration-200 inline-block"
          >
            Gửi yêu cầu tư vấn
          </a>
          <a
            href={`tel:${companyInfo.hotline.replace(/[\s+]/g, "")}`}
            class="border-2 border-white text-white px-8 py-3 rounded-lg font-semibold hover:bg-white hover:text-blue-700 transition-colors duration-200 inline-block"
          >
            Hotline: {companyInfo.hotline}
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>
